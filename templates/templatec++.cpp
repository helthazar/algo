#include <iostream>
#include <cstdio>
#include <cstring>
#include <vector>
#include <set>
#include <string>
#include <map>
#include <algorithm>
using namespace std;

#define DEBUG 0
#define dout if (DEBUG) cout
#define dopen if (DEBUG) freopen

void solve() {
    int n;
    cin >> n;
    cout << n;
}

int main() {
    dopen("input.txt", "rt", stdin);
    dopen("output.txt", "wt", stdout);
    solve();
}
